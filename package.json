{
  "name": "ghostqa-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "vitest run",
    "lint": "pnpm -r run typecheck",
    "benchmark": "tsx scripts/benchmark.ts"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "@types/node": "^25.3.3",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
